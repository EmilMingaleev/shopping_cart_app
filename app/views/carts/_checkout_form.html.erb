<form action="<%= clear_cart_path %>" method="post" onsubmit="setTimeout(function() { location.reload(); }, 0);">
    <button type="submit" class="checkout-button"><%= t('cart.checkout') %></button>
</form>
